# host 环境：运行 .claude/skills 调用的本地工具所需（无 LLM 依赖）
PyYAML>=6.0
requests>=2.28
pdfplumber>=0.10     # datasheet_extract（文本层抽取兜底）
pypdf>=4.0           # datasheet_extract 的二级兜底（pdfplumber 装不上时退化用）
# 另需系统包 graphviz(dot)：接线图渲染；缺则 render_wiring 退化为 DOT 文本
# 主机测试需 C 编译器(cc/clang/gcc)：固件中间件 host_test
