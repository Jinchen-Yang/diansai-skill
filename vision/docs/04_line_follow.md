# 第 4 模块：视觉巡线

## 写代码前先学

在 CanMV IDE for K230 v4.0.7 先运行亚博源码中的：

1. `05.Color/04.find_line.py`：基于 LAB 色块的基础巡线思路与下半画面 ROI。
2. `04.Detecting/05.linear_regression_fast.py`：直线拟合与边缘方案的概念；本模块不直接使用它。
3. `06.cv_lite/13.grayscale_threshold_binary.py`：理解二值化，观察阈值变化对黑白线条的影响。

## 本模块逻辑

```text
取 RGB565 图像
  -> 在近、中、远三条 ROI 内找黑色最大 blob
  -> 按 ROI 权重合成黑线横向中心
  -> error = 线中心 x - 画面中心 x
  -> 无有效 ROI 时 valid=0、error=0、显示 LINE LOST
```

所以约定为：黑线在画面右侧，`error > 0`；黑线在左侧，`error < 0`。未来和主控对接时只在 `valid=1` 时发送 `protocol.line_error(error)`；丢线必须单独通知主控，不能继续发上一帧旧误差。

## 运行与调参

1. 在白色硬纸板上贴约 18 至 25 mm 宽的黑色哑光胶带，先做直线，再做缓弯。
2. 将摄像头固定在车头预定高度和角度，避免手持测试后把阈值当作正式参数。
3. CanMV IDE 直接运行 `demos/04_line_follow/main.py`。
4. 调 `BLACK_LINE_THRESHOLD` 使胶带被检测而白底、阴影尽量不被检测。
5. 假线块多：提高 `MIN_AREA`，或缩小 `ROIS` 到赛道实际会出现的位置。
6. 缓弯时远处 ROI 丢失并不一定是故障；近处 ROI 能稳定得到线中心更重要。

## 通过标准

- 直线居中时 `error` 接近 0。
- 胶带移到画面右侧时 `error` 为正，左侧时为负。
- 用手遮住胶带时明确显示 `LINE LOST`，不会沿用旧误差。
- 缓弯时至少近处 ROI 能连续给出方向正确的误差。

后续联调模块会把此结果通过共享 `protocol.py` 发送给 MSPM0/STM32，并由主控负责 PID、速度和安全停车。

## 现场标定参数（比赛时必看）

| 参数 | 含义 | 什么时候调 |
| --- | --- | --- |
| `BLACK_LINE_THRESHOLD` | 黑线的 LAB 阈值 | 比赛跑道的光照、胶带哑光程度或地面颜色变化时首先调整。 |
| `ROIS` | 近、中、远三块搜线区域与各自权重 | 摄像头高度/俯仰角、跑道在画面中的位置改变时调整。 |
| `MIN_PIXELS` / `MIN_AREA` | 一块黑色 blob 被当作线的最小规模 | 阴影、轮子纹理被误当作线时上调；线远处断裂时下调。 |
| `MIN_VALID_ROIS` | 至少要有几个 ROI 找到线才认为有效 | 丢线判断太敏感或太迟钝时调整。 |

调试顺序：先固定摄像头位置 -> 调 `BLACK_LINE_THRESHOLD` -> 调 `ROIS` 使线在近区和中区稳定可见 -> 调 `MIN_PIXELS` / `MIN_AREA` 清除假线 -> 调 `MIN_VALID_ROIS` -> 最后才把误差交给主控调 PID。

## ROI 的工作逻辑

`ROI` 是感兴趣区域，不是左右分区。当前代码将 `640 x 480` 画面切成近、中、远三条横向观察带；坐标原点在左上角，`y` 越大越靠近车头。

```text
y=0
|
|  远处 ROI: (0, 245, 640, 35, 0.10)
|
|  中间 ROI: (0, 300, 640, 45, 0.30)
|
|  近处 ROI: (0, 370, 640, 55, 0.60)
|
y=480，车头附近
```

每个元组的格式是 `(x, y, 宽, 高, 权重)`。每条带内只寻找最大的黑色 blob，并取它的横向中心 `cx`。最终线位置是所有成功检测 ROI 的加权平均：

```text
line_x = Σ(roi 内黑线 cx * 该 roi 权重) / Σ(成功检测到线的 roi 权重)
error = line_x - 320
```

近处 ROI 权重最高，因为它决定车辆下一瞬间的修正方向；远处 ROI 权重较低，只提供前方走向趋势。黑线在画面右侧时 `error > 0`，左侧时 `error < 0`。当前 `MIN_VALID_ROIS = 1`，即任意一条带找到线便认为没有丢线。

## 实测状态

巡线代码的画面、ROI、加权误差和丢线逻辑已经完成并通过基础运行检查；**目前没有真实赛道，`BLACK_LINE_THRESHOLD`、`ROIS`、最小面积与丢线条件均未进行现场标定。** 在白底黑线跑道和最终摄像头安装高度确定前，不应把当前数值当作比赛定值。
